import { useEffect, useState } from "react";
import { create, getAll, remove, update } from "../../api/api";
import { useAuth } from "../../context/AuthContext";
import ConfirmDialog from "../../components/ConfirmDialog";
import FormModal from "../../components/FormModal";
import { canManageReferenceData } from "../../utils/access";

const configs = {
  categories: { label: "Organisation categories", singular: "category", id: "categoryId", name: "categoryName", description: "Classify organisations, such as public, faith-based, or private." },
  organizations: { label: "Organisations", singular: "organisation", id: "organizationId", name: "organizationName", description: "Set the organisations responsible for facilities." },
  levels: { label: "Facility levels", singular: "facility level", id: "levelId", name: "levelName", description: "Define the recognised levels of health facility." },
  creditationstatuses: { label: "Accreditation statuses", singular: "accreditation status", id: "creditationStatusId", name: "creditationStatus", description: "Describe the current accreditation position of each facility." },
};

function ReferenceDataPage() {
  const { auth } = useAuth();
  const canManage = canManageReferenceData(auth?.roleName);
  const [items, setItems] = useState({ categories: [], organizations: [], levels: [], creditationstatuses: [] });
  const [selected, setSelected] = useState("organizations");
  const [editing, setEditing] = useState(null);
  const [deleting, setDeleting] = useState(null);
  const [open, setOpen] = useState(false);
  const [loading, setLoading] = useState(true);
  const config = configs[selected];
  const load = async () => { try { setLoading(true); const responses = await Promise.all(Object.keys(configs).map(getAll)); setItems(Object.fromEntries(responses.map((response, index) => [Object.keys(configs)[index], response.data]))); } catch (error) { console.error(error); } finally { setLoading(false); } };
  useEffect(() => { load(); }, []);
  const save = async (payload) => { if (editing) await update(selected, editing[config.id], payload); else await create(selected, payload); setOpen(false); setEditing(null); await load(); };
  const destroy = async () => { await remove(selected, deleting[config.id]); setDeleting(null); await load(); };
  const rows = [...items[selected]].sort((a, b) => String(a[config.name]).localeCompare(String(b[config.name])));
  return <section aria-labelledby="reference-title"><div className="mb-6"><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700">Facility directory</p><h2 id="reference-title" className="mt-1 text-2xl font-bold tracking-tight text-[#143c42]">Reference data</h2><p className="mt-1 text-sm text-[#668187]">Set up the controlled lists used when adding facilities.</p></div><div className="mb-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{Object.entries(configs).map(([resource, item]) => <button key={resource} onClick={() => setSelected(resource)} className={`rounded-xl border p-4 text-left transition ${selected === resource ? "border-[#087c77] bg-teal-50 shadow-sm" : "border-[#e2ecea] bg-white hover:border-[#8bc8be]"}`}><p className="text-sm font-bold text-[#143c42]">{item.label}</p><p className="mt-1 text-xs text-[#668187]">{items[resource].length} records</p></button>)}</div><div className="overflow-hidden rounded-xl border border-[#e2ecea] bg-white shadow-[0_8px_24px_rgba(20,60,66,0.06)]"><div className="flex flex-col gap-3 border-b border-[#e1ecea] px-5 py-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-bold text-[#143c42]">{config.label}</h3><p className="mt-1 text-sm text-[#668187]">{config.description}</p></div>{canManage && <button onClick={() => { setEditing(null); setOpen(true); }} className="rounded-lg bg-[#087c77] px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-[#05635f]">+ Add {config.singular}</button>}</div>{loading ? <div className="space-y-3 p-6">{[1,2,3].map((item) => <div key={item} className="h-10 animate-pulse rounded bg-slate-100" />)}</div> : rows.length === 0 ? <p className="px-6 py-12 text-center text-sm text-[#668187]">No {config.label.toLowerCase()} yet.</p> : <div className="overflow-x-auto"><table className="w-full min-w-[650px] text-sm"><thead className="border-b border-[#dce9e7] bg-[#f5faf9] text-left text-xs uppercase tracking-wider text-[#527076]"><tr><th className="px-6 py-3.5">Name</th>{selected === "organizations" && <th className="px-6 py-3.5">Category</th>}{selected === "levels" && <th className="px-6 py-3.5">Order</th>}<th className="px-6 py-3.5">Description</th>{canManage && <th className="px-6 py-3.5 text-right">Actions</th>}</tr></thead><tbody className="divide-y divide-[#e7efed]">{rows.map((item) => <tr key={item[config.id]} className="hover:bg-[#f5fbfa]"><td className="px-6 py-4 font-semibold text-[#143c42]">{item[config.name]}</td>{selected === "organizations" && <td className="px-6 py-4 text-[#527076]">{item.categoryName}</td>}{selected === "levels" && <td className="px-6 py-4 text-[#527076]">{item.levelOrder}</td>}<td className="px-6 py-4 text-[#527076]">{item.description || "—"}</td>{canManage && <td className="px-6 py-4"><div className="flex justify-end gap-2"><button onClick={() => { setEditing(item); setOpen(true); }} className="rounded-md border border-[#b9d6d1] px-3 py-1.5 text-xs font-semibold text-[#087c77] hover:bg-teal-50">Edit</button><button onClick={() => setDeleting(item)} className="rounded-md border border-red-200 px-3 py-1.5 text-xs font-semibold text-red-700 hover:bg-red-50">Delete</button></div></td>}</tr>)}</tbody></table></div>}</div><FormModal open={open} onClose={() => { setOpen(false); setEditing(null); }}><ReferenceForm key={`${selected}-${editing?.[config.id] ?? "new"}`} resource={selected} initialData={editing} categories={items.categories} onSubmit={save} onCancel={() => { setOpen(false); setEditing(null); }} /></FormModal><ConfirmDialog open={!!deleting} title={`Delete ${config.singular}`} message={`Are you sure you want to delete "${deleting?.[config.name]}"?`} onConfirm={destroy} onCancel={() => setDeleting(null)} /></section>;
}

function ReferenceForm({ resource, initialData, categories, onSubmit, onCancel }) {
  const [form, setForm] = useState(initialData || { categoryId: "", levelOrder: "", description: "", comments: "" });
  const config = configs[resource]; const change = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.value }));
  const submit = (event) => { event.preventDefault(); const common = { description: form.description?.trim() || "" }; if (resource === "categories") onSubmit({ categoryName: form.categoryName?.trim(), ...common }); if (resource === "organizations") onSubmit({ organizationName: form.organizationName?.trim(), categoryId: Number(form.categoryId), ...common }); if (resource === "levels") onSubmit({ levelName: form.levelName?.trim(), levelOrder: Number(form.levelOrder), ...common }); if (resource === "creditationstatuses") onSubmit({ creditationStatus: form.creditationStatus?.trim(), ...common, comments: form.comments?.trim() || "" }); };
  const field = config.name;
  return <form onSubmit={submit} className="space-y-5"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700">Reference data</p><h2 className="mt-1 text-xl font-bold text-[#143c42]">{initialData ? "Edit" : "Add"} {config.singular}</h2></div><label className="block"><span className="mb-1.5 block text-sm font-semibold text-[#284e53]">Name</span><input required name={field} value={form[field] || ""} onChange={change} className="w-full rounded-lg border border-[#b9d6d1] px-3 py-2.5 text-sm outline-none focus:border-[#087c77] focus:ring-2 focus:ring-teal-100" /></label>{resource === "organizations" && <label className="block"><span className="mb-1.5 block text-sm font-semibold text-[#284e53]">Organisation category</span><select required name="categoryId" value={form.categoryId || ""} onChange={change} className="w-full rounded-lg border border-[#b9d6d1] bg-white px-3 py-2.5 text-sm outline-none focus:border-[#087c77] focus:ring-2 focus:ring-teal-100"><option value="" disabled>Select category</option>{categories.map((category) => <option key={category.categoryId} value={category.categoryId}>{category.categoryName}</option>)}</select></label>}{resource === "levels" && <label className="block"><span className="mb-1.5 block text-sm font-semibold text-[#284e53]">Level order</span><input required type="number" min="1" max="9" name="levelOrder" value={form.levelOrder || ""} onChange={change} className="w-full rounded-lg border border-[#b9d6d1] px-3 py-2.5 text-sm outline-none focus:border-[#087c77] focus:ring-2 focus:ring-teal-100" /></label>}<label className="block"><span className="mb-1.5 block text-sm font-semibold text-[#284e53]">Description</span><textarea name="description" rows="3" value={form.description || ""} onChange={change} className="w-full rounded-lg border border-[#b9d6d1] px-3 py-2.5 text-sm outline-none focus:border-[#087c77] focus:ring-2 focus:ring-teal-100" /></label>{resource === "creditationstatuses" && <label className="block"><span className="mb-1.5 block text-sm font-semibold text-[#284e53]">Notes</span><textarea name="comments" rows="2" value={form.comments || ""} onChange={change} className="w-full rounded-lg border border-[#b9d6d1] px-3 py-2.5 text-sm outline-none focus:border-[#087c77] focus:ring-2 focus:ring-teal-100" /></label>}<div className="flex gap-2"><button type="submit" className="rounded-lg bg-[#087c77] px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-[#05635f]">{initialData ? "Save changes" : `Add ${config.singular}`}</button><button type="button" onClick={onCancel} className="rounded-lg px-4 py-2.5 text-sm font-semibold text-[#527076] hover:bg-slate-100">Cancel</button></div></form>;
}
export default ReferenceDataPage;
