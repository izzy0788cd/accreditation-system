import { useEffect, useMemo, useState } from "react";
import { create, getAll, update } from "../../api/api";
import FormModal from "../../components/FormModal";

const inputClass = "w-full rounded-lg border border-[#b9d6d1] bg-white px-3 py-2.5 text-sm outline-none focus:border-[#087c77] focus:ring-2 focus:ring-teal-100";

function AdminUsersPage() {
  const [accounts, setAccounts] = useState([]); const [users, setUsers] = useState([]); const [roles, setRoles] = useState([]); const [organizations, setOrganizations] = useState([]);
  const [loading, setLoading] = useState(true); const [query, setQuery] = useState(""); const [status, setStatus] = useState("all"); const [creating, setCreating] = useState(false); const [editing, setEditing] = useState(null);
  const load = async () => { try { setLoading(true); const [accountsRes, usersRes, rolesRes, organizationsRes] = await Promise.all([getAll("userAccounts"), getAll("users"), getAll("roles"), getAll("organizations")]); setAccounts(accountsRes.data); setUsers(usersRes.data); setRoles(rolesRes.data); setOrganizations(organizationsRes.data); } finally { setLoading(false); } };
  useEffect(() => { load(); }, []);
  const records = useMemo(() => accounts.map((account) => { const user = users.find((item) => item.userAccountId === account.userAccountId); return { ...account, user, displayName: user ? `${user.firstName} ${user.lastName}`.trim() : "Profile pending" }; }), [accounts, users]);
  const visible = records.filter((record) => { const text = `${record.displayName} ${record.username} ${record.roleName} ${record.user?.organizationName || ""}`.toLowerCase(); return text.includes(query.toLowerCase()) && (status === "all" || (status === "active" ? record.isActive : !record.isActive)); });
  const saveNewUser = async (form) => { const account = await create("userAccounts", { username: form.username.trim(), temporaryPassword: form.temporaryPassword, roleId: Number(form.roleId) }); await create("users/admin-create", profilePayload(form, account.data.userAccountId)); setCreating(false); await load(); };
  const saveUser = async (form) => { await update("userAccounts", editing.userAccountId, { username: form.username.trim(), roleId: Number(form.roleId), isActive: form.isActive, newPassword: form.newPassword || null }); if (editing.user) await update("users", editing.user.userId, profilePayload(form)); else await create("users/admin-create", profilePayload(form, editing.userAccountId)); setEditing(null); await load(); };
  const activeCount = records.filter((record) => record.isActive).length; const pendingCount = records.filter((record) => !record.user).length;
  return <main className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><section><header className="rounded-2xl border border-[#cde5e0] bg-[linear-gradient(125deg,#e8f5f3_0%,#f8fbfa_70%)] p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700">Administration</p><div className="mt-2 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-bold tracking-tight text-[#143c42]">User management</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#527076]">Create accounts, maintain profiles, assign roles, and manage account credentials.</p></div><button onClick={() => setCreating(true)} className="rounded-lg bg-[#087c77] px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-[#05635f]">+ Add user</button></div></header><div className="mt-6 grid gap-4 sm:grid-cols-3"><Metric label="User accounts" value={records.length} /><Metric label="Active access" value={activeCount} /><Metric label="Profiles pending" value={pendingCount} /></div><section className="mt-6 overflow-hidden rounded-xl border border-[#e2ecea] bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-[#e1ecea] p-5 lg:flex-row lg:items-center lg:justify-between"><div><h2 className="font-bold text-[#143c42]">Accounts and access</h2><p className="mt-1 text-sm text-[#668187]">Only Administrators can manage users and roles.</p></div><div className="flex gap-2"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search users" className={inputClass} /><select value={status} onChange={(event) => setStatus(event.target.value)} className={inputClass}><option value="all">All statuses</option><option value="active">Active</option><option value="inactive">Inactive</option></select></div></div>{loading ? <div className="space-y-3 p-6">{[1, 2, 3].map((item) => <div key={item} className="h-14 animate-pulse rounded bg-slate-100" />)}</div> : <div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="border-b border-[#dce9e7] bg-[#f5faf9] text-xs uppercase tracking-wider text-[#527076]"><tr><th className="px-6 py-3.5">User</th><th className="px-6 py-3.5">Organisation</th><th className="px-6 py-3.5">Role</th><th className="px-6 py-3.5">Access</th><th className="px-6 py-3.5 text-right">Actions</th></tr></thead><tbody className="divide-y divide-[#e7efed]">{visible.map((record) => <tr key={record.userAccountId}><td className="px-6 py-4"><p className="font-semibold text-[#143c42]">{record.displayName}</p><p className="mt-0.5 text-xs text-[#668187]">@{record.username}{record.user?.email ? ` · ${record.user.email}` : ""}</p></td><td className="px-6 py-4 text-[#527076]">{record.user?.organizationName || "—"}</td><td className="px-6 py-4 text-[#527076]">{record.roleName}</td><td className="px-6 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${record.isActive ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-600"}`}>{record.isActive ? "Active" : "Inactive"}</span></td><td className="px-6 py-4 text-right"><button onClick={() => setEditing(record)} className="rounded-md border border-[#b9d6d1] px-3 py-1.5 text-xs font-semibold text-[#087c77] hover:bg-teal-50">Manage</button></td></tr>)}</tbody></table></div>}</section></section><FormModal open={creating} onClose={() => setCreating(false)}><UserForm roles={roles} organizations={organizations} onSubmit={saveNewUser} onCancel={() => setCreating(false)} /></FormModal><FormModal open={!!editing} onClose={() => setEditing(null)}><UserForm editing={editing} roles={roles} organizations={organizations} onSubmit={saveUser} onCancel={() => setEditing(null)} /></FormModal></main>;
}

const profilePayload = (form, userAccountId) => ({ ...(userAccountId ? { userAccountId } : {}), firstName: form.firstName.trim(), lastName: form.lastName.trim(), organizationId: Number(form.organizationId), position: form.position.trim(), email: form.email.trim(), phone: form.phone.trim(), mobile: form.mobile.trim(), comments: form.comments.trim() });
const Metric = ({ label, value }) => <div className="rounded-xl border border-[#e2ecea] bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wider text-[#668187]">{label}</p><p className="mt-2 text-3xl font-bold text-teal-700">{value}</p></div>;

function UserForm({ editing, roles, organizations, onSubmit, onCancel }) {
  const account = editing || {}; const [form, setForm] = useState({ username: account.username || "", temporaryPassword: "", newPassword: "", roleId: roles.find((role) => role.roleName === account.roleName)?.roleId || "", isActive: account.isActive ?? true, firstName: account.user?.firstName || "", lastName: account.user?.lastName || "", organizationId: account.user?.organizationId || "", position: account.user?.position || "", email: account.user?.email || "", phone: account.user?.phone || "", mobile: account.user?.mobile || "", comments: account.user?.comments || "" });
  const change = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.type === "checkbox" ? event.target.checked : event.target.value }));
  return <form onSubmit={(event) => { event.preventDefault(); onSubmit(form); }} className="space-y-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700">Administration</p><h2 className="mt-1 text-xl font-bold text-[#143c42]">{editing ? "Manage user" : "Add user"}</h2><p className="mt-1 text-sm text-[#668187]">{editing ? "Update credentials, access, and profile details." : "Create an account and profile."}</p></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Username"><input required name="username" value={form.username} onChange={change} className={inputClass} /></Field><Field label={editing ? "New password" : "Temporary password"}><input required={!editing} minLength="8" type="password" name={editing ? "newPassword" : "temporaryPassword"} value={editing ? form.newPassword : form.temporaryPassword} onChange={change} placeholder={editing ? "Leave blank to keep current password" : ""} className={inputClass} /></Field></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Role"><select required name="roleId" value={form.roleId} onChange={change} className={inputClass}><option value="" disabled>Select role</option>{roles.map((role) => <option key={role.roleId} value={role.roleId}>{role.roleName}</option>)}</select></Field>{editing && <label className="mt-7 flex items-center gap-2 text-sm font-semibold text-[#284e53]"><input type="checkbox" name="isActive" checked={form.isActive} onChange={change} className="h-4 w-4 accent-[#087c77]" /> Account is active</label>}</div><div className="grid gap-4 sm:grid-cols-2"><Field label="First name"><input required name="firstName" value={form.firstName} onChange={change} className={inputClass} /></Field><Field label="Last name"><input required name="lastName" value={form.lastName} onChange={change} className={inputClass} /></Field></div><Field label="Organisation"><select required name="organizationId" value={form.organizationId} onChange={change} className={inputClass}><option value="" disabled>Select organisation</option>{organizations.map((organization) => <option key={organization.organizationId} value={organization.organizationId}>{organization.organizationName}</option>)}</select></Field><Field label="Position"><input name="position" value={form.position} onChange={change} className={inputClass} /></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Email"><input required type="email" name="email" value={form.email} onChange={change} className={inputClass} /></Field><Field label="Phone"><input required name="phone" value={form.phone} onChange={change} className={inputClass} /></Field></div><Field label="Mobile"><input name="mobile" value={form.mobile} onChange={change} className={inputClass} /></Field><div className="flex justify-end gap-2 pt-2"><button type="button" onClick={onCancel} className="rounded-lg px-4 py-2.5 text-sm font-semibold text-[#527076] hover:bg-slate-100">Cancel</button><button type="submit" className="rounded-lg bg-[#087c77] px-4 py-2.5 text-sm font-semibold text-white hover:bg-[#05635f]">{editing ? "Save changes" : "Create user"}</button></div></form>;
}

const Field = ({ label, children }) => <label className="block"><span className="mb-1.5 block text-sm font-semibold text-[#284e53]">{label}</span>{children}</label>;
export default AdminUsersPage;
